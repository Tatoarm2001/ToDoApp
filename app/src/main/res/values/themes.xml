<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Proyecto6TODOList" parent="Theme.Material3.DayNight.NoActionBar">

    </style>

    <style name="Theme.Proyecto6TODOList" parent="Base.Theme.Proyecto6TODOList" >
        <!-- Primary brand color. -->
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/todo_background_todo_app</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="TodoSubtitle">
        <item name="textAllCaps">true</item>
        <item name="android:textColor">@color/todo_subtitle_text</item>
        <item name="android:layout_marginHorizontal">32dp</item>
    </style>

    <style name="Subtitle">
        <item name="textAllCaps">true</item>
        <item name="android:textColor">@color/todo_subtitle_text</item>
        <item name="marginHorizontal">32dp</item>
    </style>

</resources>